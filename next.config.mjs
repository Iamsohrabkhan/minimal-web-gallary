/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["cdn.sanity.io","images.unsplash.com","assets-global.website-files.com"]
     }
};

export default nextConfig;
